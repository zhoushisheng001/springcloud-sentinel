server:
  port: 8091
  servlet:
    context-path: /sentinel
spring:
  application:
    name: spring-sentinel
  cloud:
#    nacos:
#      discovery:
#        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8080
        port: 8719
management:
  endpoints:
    web:
      exposure:
        include: "*"
